# Aufgabe 1 — HTML5 Semantik

Blumen-Galerie als semantisches HTML5-Grundgerüst, ohne CSS. Nutzt `<nav>`, `<main>`,
`<section>` und `<article>` statt generischer `<div>`s, mit Sprungmarken-Navigation zu den
einzelnen Farbgruppen (rot, blau, lila, pink, gelb, bunt).

`blume.html` im Browser öffnen.
