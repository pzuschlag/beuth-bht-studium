# Aufgabe 9b — SASS Mixins (Shapes-Layout)

Dieselbe Übung wie [`../aufgabe-09a-…-grid/`](../aufgabe-09a-sass-mixins-grid/), angewendet auf
das Shapes-Layout. Enthält zusätzlich ein `bg-gardient-from-top-to-bottom($topColor,
$bottomColor)`-Mixin für Farbverläufe und eine `lower-opacity($color, $percentage)`-Funktion.

`blume.html` im Browser öffnen, Quelle in `styles/styles.scss`.
