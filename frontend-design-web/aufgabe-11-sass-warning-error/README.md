# Aufgabe 11 — SASS Warning & Error

`@warn`, ausgelöst innerhalb einer Schleife, um Compile-Zeit-Diagnosen in SASS zu demonstrieren
(`@error` ist als Alternative auskommentiert vorhanden).

`blume.html` im Browser öffnen, Quelle in `styles/styles.scss`.
