# Aufgabe 9a — SASS Mixins (Grid-Layout)

Aufbauend auf Aufgabe 8a: wiederkehrende Stile als `@mixin` ausgelagert, z. B. `border-solid`
und `border-dotted` für Rahmenvarianten, per `@include` an mehreren Stellen wiederverwendet.

`blume.html` im Browser öffnen, Quelle in `styles/styles.scss`.
