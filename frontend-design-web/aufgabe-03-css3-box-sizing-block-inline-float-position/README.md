# Aufgabe 3 — CSS3 box-sizing, block/inline, float, position

Layout-Übung mit schriftlichen Erklärungen als Kommentare direkt im CSS: `box-sizing:
border-box` und seine Auswirkung auf das Boxmodell, `float: left` für Bild-Umflüsse, sowie
`position: relative/absolute/fixed` im Vergleich.

`blume.html` im Browser öffnen, Styles in `styles/styles.css`.
