# Aufgabe 6 — CSS3 Shapes

"Blumengrüße"-Seite mit Textumfluss um nicht-rechteckige Formen: `shape-outside: polygon(...)`
für einen diagonalen Textfluss neben einem Blumenbild (auskommentiert: Alternativen mit
`circle()` und einer Bild-Maske via `shape-outside: url(...)`).

`blume.html` im Browser öffnen, Styles in `styles/styles.css`.
