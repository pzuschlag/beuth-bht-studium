# Aufgabe 2 — CSS3 Selektoren, Spezifität, strukturelle Pseudoklassen

Dieselbe Blumen-Galerie aus Aufgabe 1, jetzt gestylt über strukturelle Pseudoklassen statt
Klassen: `:nth-child(even)`, `:nth-child(3n+1)`, `:nth-child(3n+2)`, `:not(:last-child)` und
`:after` für generierten Inhalt.

`blume.html` im Browser öffnen, Styles in `styles/styles.css`.
