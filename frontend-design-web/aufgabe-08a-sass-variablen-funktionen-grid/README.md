# Aufgabe 8a — SASS Variablen & Funktionen (Grid-Layout)

Das Grid-Layout aus Aufgabe 5 nach SASS migriert: Farb-/Abstandswerte als `$variablen`
(z. B. `$border-color`), Farbberechnungen über eine eigene `@function`. Kompiliertes CSS liegt
als `styles.css` daneben.

`blume.html` im Browser öffnen, Quelle in `styles/styles.scss`.
