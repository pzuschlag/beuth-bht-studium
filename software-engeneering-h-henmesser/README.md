# Software Engineering — Höhenmesser

Android-App: Höhenmesser. Einzelarbeit.

**Stack:** Android (Java), Gradle

## Inhalt

- `app/src/` — Android-Quellcode
- `app/build/` — Build-Artefakte (Android AAR-Bibliotheken)
- `build.gradle`, `settings.gradle` — Gradle-Build-Konfiguration

> Hinweis: `app/build/` enthält generierte Build-Artefakte und würde
> normalerweise per `.gitignore` ausgeschlossen.
