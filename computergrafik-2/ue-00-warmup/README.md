# Warmup: Pfad-Objekte in JavaScript

Einstiegsübung vor dem eigentlichen Canvas/WebGL-Stoff: eine `makePath`-Fabrikfunktion, die
Pfad-Strings aus einzelnen Punkten zusammensetzt (Closures, funktionale Grundlagen in JS).

`main.js` — kein HTML, läuft als Node-Skript oder über die Browser-Konsole.
